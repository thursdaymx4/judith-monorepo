<?xml version="1.0" encoding="utf-8"?>
<!--
  Declares the Judith home-screen widget for the OS picker. updatePeriodMillis=0
  because the app pushes updates itself (writePayload → AppWidgetManager) rather
  than relying on the system's (min 30-min) refresh cadence.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
  android:minWidth="180dp"
  android:minHeight="110dp"
  android:targetCellWidth="4"
  android:targetCellHeight="2"
  android:updatePeriodMillis="0"
  android:initialLayout="@layout/judith_widget"
  android:previewLayout="@layout/judith_widget"
  android:resizeMode="horizontal|vertical"
  android:widgetCategory="home_screen"
  android:description="@string/judith_widget_description" />
